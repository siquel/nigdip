# nigdip 

## Setup
Place script files to *plugins* folder

## Hooks
```pl
sub onLoad {
  # called when script is loaded
}

sub onUnload {
  # called when script is unloaded
}

sub onDisable {
  # called when script is disabled
}

sub onEnable {
  # called when script is enabled
}
```
